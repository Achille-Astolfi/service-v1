import { LoginTemplateForm } from './login-template-form';

describe('LoginTemplateForm', () => {
  it('should create an instance', () => {
    expect(new LoginTemplateForm()).toBeTruthy();
  });
});
